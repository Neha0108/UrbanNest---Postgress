export interface Product {
  expectedDelivery: string;
  productId: number;
  productName: string;
  productPrice: number;
  imagepath?: string[]; 
  productDescription: string; 
  stock: number;         
  CategoryName: string;
  SubCategoryName: string;
  };
